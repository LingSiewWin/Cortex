# dependencies (bun install)
node_modules

# output (anchored to repo root so cortex-plugin/dist — the standalone plugin
# bundles that MUST ship for marketplace installs — is NOT ignored)
/out
/dist
*.tgz

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
*.tsbuildinfo

# Next.js (distDir: dist in next.config.ts for Vercel output path).
# Anchored to repo root — an unanchored `dist/`/`out/` would also match
# cortex-plugin/dist (the standalone plugin bundles that MUST ship) and silently
# drop newly-hashed connect assets from commits + the CI dist-check.
.next/
/dist/
/out/

# Copied at build time from assets/ (see package.json build script)
public/

# Editor — local ports / machine-specific
.vscode/

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store

# Private research notes (bulk). Public docs are whitelisted below.
docs/**
!docs/README.md
!docs/submission/
!docs/submission/**
!docs/Arkiv.md
!docs/ERC.md
!docs/MIRROR.md
!docs/WALKTHROUGH.md
!docs/Wallet.md
!docs/RabitQ.md

# private agent / IDE config
CLAUDE.md
.cursor/
.agents/
info.md

# Local SQLite mirror — never commit (ships personal observations + citations)
cortex-mirror.sqlite
cortex-mirror.sqlite-wal
cortex-mirror.sqlite-shm
cortex-mirror.test-*.sqlite*
cortex-console.png
.playwright-mcp/

# B4 ablation embedding cache (regenerable)
scripts/eval/.embed-cache.json

scripts/eval/.unkillable-log.jsonl
.vercel
