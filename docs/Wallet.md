1. The Standards Correction: Who uses what?Phantom / MetaMask (EOAs): Because they are standard wallets, they natively cannot batch transactions. To fix this, the Ethereum network introduced EIP-7702. This standard allows MetaMask to temporarily "upgrade" into a smart contract for a single transaction just so it can execute a batch.  Safe / Coinbase Smart Wallet / Argent (Smart Wallets): These are already smart contracts, so they don't need EIP-7702. They have been able to batch transactions for years using the ERC-4337 account abstraction standard.The App (ERC-5792): The app you are using (like Uniswap or your AI agent dashboard) uses ERC-5792 to ask the wallet: "Hey, can you batch transactions?"If Coinbase Wallet answers, it says, "Yes, natively via ERC-4337."If MetaMask answers, it says, "Yes, temporarily via EIP-7702."The Universal Login (ERC-1271 + ERC-6492): You nailed this. If an app builds this correctly, it doesn't matter if you connect an EOA (MetaMask) or a Smart Wallet (Safe). The app accepts signatures from both seamlessly.2. How Turnkey Works (Which ERC?)Turnkey isn't a wallet standard or an ERC itself—it is a Key Management System (KMS) built on Trusted Execution Environments (TEEs), specifically AWS Nitro Enclaves.  When you create a Turnkey wallet, the TEE generates a standard Ethereum private key (an EOA). However, because you want the cool features like zero gas fees and batched transactions, developers almost always take that Turnkey EOA and use it as the "owner" of an ERC-4337 Smart Wallet.Your Passkey unlocks the Turnkey TEE -> The TEE signs a transaction -> The transaction is sent through the ERC-4337 smart wallet.3. The Centralization Paradox (Your AI Memory)Your logic here is absolutely flawless. You have identified the exact compromise of modern "embedded" Web3 wallets.Turnkey is technically non-custodial. The private key is generated inside a secure hardware chip (the TEE) and the Turnkey engineers cannot extract it. They cannot steal your assets or your AI memory.  